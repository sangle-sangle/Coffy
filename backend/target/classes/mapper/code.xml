<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssafy.code">
	<select id="getCodes" parameterType="java.lang.Integer"
		resultType="com.ssafy.edu.vue.dto.Code">
		select id, title, description, html, css, javascript, writerid, created_at 
		from code
		order by id	desc
	</select>

	<select id="getCode" parameterType="java.lang.Integer"
		resultType="com.ssafy.edu.vue.dto.Code">
		select id, title, description, html, css, javascript, writerid, created_at
		from code
		where id=#{id}
	</select>

	<insert id="addCode" parameterType="com.ssafy.edu.vue.dto.Code">
		insert into code(title, description, html, css, javascript, writerid, created_at)
		values(#{title}, #{description}, #{html}, #{css}, #{javascript}, #{writerid}, now())
	</insert>
	
	<update id="updateCode"
		parameterType="com.ssafy.edu.vue.dto.Code">
		update code set
		title=#{title},
		description=#{description},
		html=#{html},
		css=#{css},
		html=#{html},
		javascript=#{javascript},
		writerid=#{writerid}
		where id=#{id}
	</update>

	<delete id="deleteCode" parameterType="java.lang.Integer">
		delete from code where id=#{id}
	</delete>
</mapper>